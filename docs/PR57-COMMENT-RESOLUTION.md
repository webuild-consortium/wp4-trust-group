# PR #57 Comment Resolution – paste in each review comment

Copy the line below into the matching GitHub comment thread, then resolve.

---

## lj-raidiam – Solution Overview (line 9, scope/registry)

```
Addressed in 730d727 — fix(pr57): limit scope to TL automation, add registry rule and onboarding data-only
```

---

## lj-raidiam – items 3–4 (line 13, external TLPs/static CAs)

```
Addressed in 7b634b0 — fix(pr57): Wallet Provider TL, WP4 member TLs, external TLPs, baseline CA on Access/Reg TLs
```

---

## lj-raidiam – onboarding data-only (line 136)

```
Addressed in 730d727 — fix(pr57): limit scope to TL automation, add registry rule and onboarding data-only
```

---

## lj-raidiam – Test platform scope (line 162)

```
Addressed in d0dba31 — fix(pr57): split test compliance CD to separate PR
```

---

## lj-raidiam – CA on Access/Reg TLs (line 179)

```
Addressed in 7b634b0 — fix(pr57): Wallet Provider TL, WP4 member TLs, external TLPs, baseline CA on Access/Reg TLs
```

---

## lj-raidiam – WP4 member TLs (line 301)

```
Addressed in 7b634b0 — fix(pr57): Wallet Provider TL, WP4 member TLs, external TLPs, baseline CA on Access/Reg TLs
```

---

## lalc – Agree with above (line 9)

```
Addressed in 730d727 — fix(pr57): limit scope to TL automation, add registry rule and onboarding data-only
```

---

## lalc – wallet-provider, ebwoid-provider (line 125)

```
Addressed in b86fa1c — fix(pr57): add wallet-provider and ebwoid-provider
```

---

## lalc – Wallet Provider TL (line 275)

```
Addressed in 7b634b0 — fix(pr57): Wallet Provider TL, WP4 member TLs, external TLPs, baseline CA on Access/Reg TLs
```

---

## lalc – RSA 3072 bits (line 89)

```
Addressed in 149af4c — fix(pr57): RSA 3072 bits per ETSI TS 119 312
```

---

## lalc – Mermaid diagram

```
Addressed in 389c4ef — fix(pr57): add Mermaid diagram for overall process
```

---

## esthermakaay – ETSI versions, TLv6, validation tests

```
Addressed in 3e530b8, a1deaa3 — ETSI versions (TLv5/TLv6), eIDAS validation tests ref
```

---

## esthermakaay – WE BUILD practical

```
Addressed in 629bf00 — fix(pr57): add WE BUILD practical documentation
```

---

# Branches

- **PR57 fixes**: `pr57-review-fixes` (9 commits)
- **Draft – test compliance**: `test-compliance-cd-draft`

Push and open PRs:

```bash
git push origin pr57-review-fixes
git push origin test-compliance-cd-draft
```

Then open PR for `pr57-review-fixes` → `main` (or `sdk`), and a draft PR for `test-compliance-cd-draft` → `main` (or `sdk`).
